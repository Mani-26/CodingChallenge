package Week2;
import java.util.*;
public class Question15 {
    public static void main(String[] args) {
        Scanner sc=new Scanner(System.in);
        String str=sc.nextLine();sc.close();
        // char A,E,I,O,U,a,e,i,o,u;
        for (int k = 0; k < str.length(); k++) {
            // if(str.charAt(k)==a ||str.charAt(k)==e ||str.charAt(k)==i ||str.charAt(k)==o ||str.charAt(k)==u ||str.charAt(k)==A ||str.charAt(k)==E ||str.charAt(k)==I ||str.charAt(k)==O ||str.charAt(k)==U)
            //     continue;
            
        }
    }
}
